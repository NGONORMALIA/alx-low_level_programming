#include <stdio.h>

/**
 * main - Entry point
 * @argc: the number of line command
 * @argv: the command line
 * Return: Not
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

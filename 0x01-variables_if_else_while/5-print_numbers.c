#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point to print single digit number in base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{ printf("%d", i); }
	putchar('\n');
	return (0);
}
